# Copy to .env.server and fill in. Never commit .env.server.
#
#   cp .env.server.example .env.server
#   make up

# Grafana admin account, created on first boot only. Changing it later has no
# effect — change the password in the UI instead.
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=

# Must match how Grafana is reached, or share/alert links point at the wrong
# host. Becomes https://grafana.${OBS_DOMAIN} once Traefik is in front (M6).
GF_SERVER_ROOT_URL=http://localhost:3000

# Traefik and Let's Encrypt (M6)
#OBS_DOMAIN=example.com
#ACME_EMAIL=you@example.com
